import t from "./Tile.js"; export default class e { static BOTTOM = 0; static COLLISION = 1; static TOP = 2; constructor(t, i) { this.tiles = e.generateTiles(t.data, i), this.width = t.width, this.height = t.height } render() { for (let t = 0; t < this.height; t++)for (let e = 0; e < this.width; e++)this.getTile(e, t)?.render(e, t) } getTile(t, e) { return this.tiles[t + e * this.width] } isTileGrass(e, i) { return this.getTile(e, i).id === t.GRASS } static generateTiles(e, i) { let s = []; return e.forEach(e => { e--; let h = -1 === e ? null : new t(e, i); s.push(h) }), s } };